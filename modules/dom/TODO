Дописать интерфейсы событий https://www.w3.org/TR/uievents/

И еще остальные дописать

Прикрепить к классам умные указатели (в DOM они обязательно нужны)

Надо доделать CSSRuleBuilder для CSSStyleSheet::insertRule

Надо сделать ElementBuilder для Document::createElement

Надо сделать EventBuilder для Document::createEvent

Убрать все поля из классов и сделать, чтобы переменные, которые возращали поля, возращали атрибуты

== Выполнено ==

Сделать другое выделение. Чтобы вставить элемент за node, нужно написать setEnd(node, 0),
а не setEnd(node->nextSibling(), 0). Еще надо убрать mCursorEnd и сделать, чтобы можно было
вставить элемент за самый первый дочерний узел
